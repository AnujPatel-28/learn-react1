import React from 'react'

const App = () => {
 /*e.preventDEfault here prevents its default behaviour of form to be page reload after the submit
  const submitHandeler = (e) => {
    e.preventDefault();
    console.log("submit");
  }

  <form onSubmit= {(e)=>{submitHandeler}}>
        <input type="text" placeholder="Enter your name"/>
        <button>Submit</button>
  </form>

  now we will for two way binding for the inputs 
    */
  return (
    <div>
      
    </div>
  )
}

export default App
